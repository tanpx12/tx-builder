# Context

I have a NEAR contract deployed on NEAR testnet at `testnet-deployer.testnet`. This contract ultilize chain signature to perform actions crosschain. Source code of the contract can be found at `../asset-manager/` folder
